#pragma once

// Transitional forwarding header: the CPU primitives moved to
// arch/x86_64/asm.h. Removed once common code includes the HAL headers
// (hal/cpu.h etc.) instead.
#include <arch/x86_64/asm.h>
