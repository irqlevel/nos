[package]
name = "r8168"
version = "0.1.0"
edition = "2021"

[lib]

[dependencies]
kcore = { path = "../../kcore" }
# The device table a NIC registers in, and the frames it moves.
net = { path = "../../net" }
