[package]
name = "virtio-net"
version = "0.1.0"
edition = "2021"

[lib]
name = "virtio_net"

[dependencies]
kcore = { path = "../../kcore" }
# The device table a NIC registers in, and the frames it moves.
net = { path = "../../net" }
virtio = { path = "../../virtio" }
