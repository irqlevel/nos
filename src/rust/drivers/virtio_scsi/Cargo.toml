[package]
name = "virtio-scsi"
version = "0.1.0"
edition = "2021"

[lib]
name = "virtio_scsi"

[dependencies]
kcore = { path = "../../kcore" }
# The device table a disk is registered in.
block = { path = "../../block" }
virtio = { path = "../../virtio" }
