[package]
name = "virtio-blk"
version = "0.1.0"
edition = "2021"

[lib]
name = "virtio_blk"

[dependencies]
kcore = { path = "../../kcore" }
# The device table a disk is registered in.
block = { path = "../../block" }
virtio = { path = "../../virtio" }
