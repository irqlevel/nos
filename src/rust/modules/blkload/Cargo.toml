[package]
name = "mod-blkload"
version = "0.1.0"
edition = "2021"

[lib]
name = "mod_blkload"
crate-type = ["staticlib"]

[dependencies]
kmod = { path = "../../kmod" }
kcore = { path = "../../kcore" }
