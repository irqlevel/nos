[package]
name = "fs"
version = "0.1.0"
edition = "2021"

[lib]

[dependencies]
kcore = { path = "../kcore" }
# The disks a filesystem is mounted on: the block layer itself, not the C ABI
# a module reaches it by.
block = { path = "../block" }
