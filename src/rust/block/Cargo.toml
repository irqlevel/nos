[package]
name = "block"
version = "0.1.0"
edition = "2021"

[lib]

[dependencies]
kcore = { path = "../kcore" }
# The table below implements the C ABI the ffi crate declares, so it takes
# the layout of an ops table from there rather than restating it.
ffi = { path = "../ffi" }
